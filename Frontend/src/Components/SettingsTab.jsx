import React from "react";

const SettingsTab = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold mb-2">Account Settings</h2>
      <p>Manage preferences, notifications, language, theme, and more.</p>
    </div>
  );
};

export default SettingsTab;